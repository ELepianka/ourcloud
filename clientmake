OBJECTS = csapp.o client.o
HEADERS = csapp.h 
CFLAGS = -g
LIBS = -lpthread

all: $(OBJECTS) 
	gcc $^ $(CFLAGS) $(LIBS) -o client

%.o: %.c $(HEADERS)
	gcc -c $(CFLAGS) $< -o $@ 

clean:
	rm -f $(OBJECTS)
